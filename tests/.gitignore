# Keep test images local (too large for git)
images/**/*.jpg
images/**/*.jpeg
images/**/*.png
images/**/*.webp
images/**/*.gif
images/**/*.heic
images/**/*.heif

# Keep results local
results/*.json

# But track the folder structure
!images/.gitkeep
!images/*/.gitkeep
